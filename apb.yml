version: 1.0
name: customeronboard
description: This is a sample application generated by apb init
bindable: False
async: optional
metadata:
  displayName: customer-onboard
  dependencies: []
plans:
  - name: gold
    description: This plan setup the default resource allocation
    free: True
    metadata: {}
    parameters:
      - name: project_name
        description: Project name
        type: string
        default: "newproject"
        required: true
      - name: admin_users
        description: Admin users to be added for project, comma separated values
        type: string
        default: "manojbadam,debianmaster"
        required: true
        updatable: true
      - name: readonly_users
        description: Read only users to be added for project, comma separated values
        type: string
        default: "karthik"
        required: true
        updatable: true        
      - name: project_cpu
        description: CPU limit for the Project
        type: number
        default: 4
        required: true
        updatable: false
      - name: project_mem
        description: Memory limit for the Project
        type: number
        default: 8
        required: true
        updatable: false
      - name: num_services
        description: Number of services allowed
        type: number
        default: 4
        required: true
        updatable: false                          
  - name: platinum
    description: This plan setup the platinum resource allocation
    free: False
    metadata: {}
    parameters:
      - name: project_name
        description: Project name
        type: string
        default: "newproject"
        required: true
      - name: admin_users
        description: Admin users to be added for project, comma separated values
        type: string
        default: "manojbadam,debianmaster"
        required: true
        updatable: true
      - name: readonly_users
        description: Read only users to be added for project, comma separated values
        type: string
        default: "karthik"
        required: true
        updatable: true        
      - name: project_cpu
        description: CPU limit for the Project
        type: number
        default: 8
        required: true
        updatable: false
      - name: project_mem
        description: Memory limit for the Project
        type: number
        default: 16
        required: true
        updatable: false
      - name: num_services
        description: Number of services allowed
        type: number
        default: 8
        required: true
        updatable: false   
  - name: diamond
    description: This plan setup the diamond resource allocation
    free: False
    metadata: {}
    parameters:
      - name: project_name
        description: Project name
        type: string
        default: "newproject"
        required: true
      - name: admin_users
        description: Admin users to be added for project, comma separated values
        type: string
        default: "manojbadam,debianmaster"
        required: true
        updatable: true
      - name: readonly_users
        description: Read only users to be added for project, comma separated values
        type: string
        default: "karthik"
        required: true
        updatable: true        
      - name: project_cpu
        description: CPU limit for the Project
        type: number
        default: 16
        required: true
        updatable: false
      - name: project_mem
        description: Memory limit for the Project
        type: number
        default: 32
        required: true
        updatable: false
      - name: num_services
        description: Number of services allowed
        type: number
        default: 16
        required: true
        updatable: false           